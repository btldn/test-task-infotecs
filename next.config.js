/** @type {import('next').NextConfig} */

const nextConfig = {
  output: 'export',
  basePath: '/test-task-infotecs',
  assetPrefix: '/test-task-infotecs',
  trailingSlash: true,
}

module.exports = nextConfig;
